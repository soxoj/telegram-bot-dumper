# Telegram API credentials from https://my.telegram.org
# Needed only for LIVE capture (CLI dumping / webapp live mode).
# Viewing an existing dump in the webapp needs neither.
# Copy to .env and fill in:  cp .env.example .env
API_ID=
API_HASH=
